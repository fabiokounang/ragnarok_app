/**
 * Grimoire public API — backed by MySQL (see models/grimoireModel.js).
 * Run: npm run db:migrate-grimoire && npm run db:seed-grimoire
 */
'use strict';

module.exports = require('../models/grimoireModel');
